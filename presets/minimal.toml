# Lingmo Forge Minimal Build Preset
# 最小化构建 - 仅包含基础系统

arch = "amd64"
suite = "bookworm"

[platform]
name = "debian"
components = ["main"]

[image]
engine = "livebuild"
iso_name = "lingmo-minimal.iso"
volume_id = "Lingmo Minimal"
